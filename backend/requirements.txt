# 依赖以 uv 管理（单一真相源 = pyproject.toml + uv.lock）。
# 本文件是不用 uv 的环境的备用清单；内容与 pyproject.toml [dependencies] 保持一致。
# 建议使用 uv：
#     cd backend && uv sync --extra dev
# 纯 pip 备用：
#     pip install -r requirements.txt && pip install pytest
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
httpx>=0.27
openai>=3.8.0
python-multipart>=0.0.9
python-dotenv>=1.0